{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo skip"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node -e \"require('ts-node').register(); require('./prisma/seed.ts')\" && node dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}